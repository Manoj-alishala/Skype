apiVersion: v1
kind: Service
metadata:
  name: chat-app-service
  namespace: chat-app
spec:
  selector:
    app: chat-app
  type: LoadBalancer   # Use NodePort for local clusters (minikube/kind)
  ports:
    - protocol: TCP
      port: 80          # External port
      targetPort: 5000  # Container port
